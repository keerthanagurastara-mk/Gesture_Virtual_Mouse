# Camera settings
CAMERA_WIDTH = 640
CAMERA_HEIGHT = 480

# Screen padding to limit cursor movement area
SCREEN_PADDING = 50

# Click detection threshold (distance between index & thumb)
CLICK_DISTANCE = 30

# Mouse smoothing factor (higher = smoother, slower)
SMOOTHING_FACTOR = 7
